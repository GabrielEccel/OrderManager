import { View } from "react-native";
import Header  from "../../components/Header";

export default function Accounts() {

    return (
        <View style={{flex: 1}}>
            <Header label="Contas"/>
        </View>

    );
}